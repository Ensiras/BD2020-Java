package JPA.validation;

import javax.persistence.EntityManager;
import javax.persistence.Persistence;
import java.util.GregorianCalendar;

public class CarApp {
    public static void main(String[] args) {
        /*EntityManager em = Persistence.createEntityManagerFactory("MySQL").createEntityManager();
        em.getTransaction().begin();
        Car car = new Car();
        car.setBrand("Honda");
        car.setEntryIntoService(new GregorianCalendar(1980, 9, 9));
        car.setListPrice(1000.34);
        car.setPlate("1-DDD-123");
        car.setPower(300);
        car.setType("Hatchback");

        em.persist(car);
        em.getTransaction().commit();*/
    }
}
